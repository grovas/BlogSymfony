{% extends 'base.html.twig' %}

{% block body %}
	{% block menu %} {% endblock %}
	<table class="table">
		<tbody>
			<tr>
				<th>Numer </th>
				<th>Data utworzenia </th>
				<th>Tytuł </th>
				<th>Post</th>
				<th>Załącznik</th>
				<th>User</th>
			</tr>omposer require phpunit/phpunit-selenium
			{% for post in posts %}
				<tr>
					<td>
						{{ post.id }}
					</td>
					<td>
						{{ post.date|date('Y-m-d') }}
					</td>
					<td>
						<a href="{{ path('post_view_index', {'id': post.id}) }}">
						{{ post.title }}
						</a>
					</td>
					<td>
						<a href="{{ path('post_view_index', {'id': post.id}) }}">
							{{ post.body | length > 25 ? post.body|slice(0,25)~'...' : post.body }}
						</a>
					</td>
					<td>
						<a href="{{ asset('uploads/attachments/' ~ post.attachment) }}">
							{{ post.attaOriginName }}</a>
					</td>>
					<td>
						{{ user.getUsername }}
					</td>
					<td>
						<a href="{{ path('post_edit_index', {'id': post.id}) }}">
							Edycja posta
						</a>
					</td>
					<td>
						<a href="{{ path('post_delete_index', {'id': post.id}) }}">
							Usunięcie posta
						</a>
					</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endblock %}